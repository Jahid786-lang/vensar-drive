# Backend API base URL (NestJS server)
VITE_API_URL=http://localhost:3000

# Optional: strong key for encrypting auth token in sessionStorage (production me zaroor set karein)
# VITE_AUTH_ENC_KEY=your-32-char-or-longer-secret-key
