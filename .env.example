# ============================================================
# Vensar Drive – Frontend Environment Variables (EXAMPLE)
# ============================================================
# 1. Is file ko copy karo:  cp .env.example .env
# 2. .env mein apni values fill karo
# 3. .env file KABHI commit mat karo (git mein .gitignore mein hai)
# ============================================================

# ── Backend API ──────────────────────────────────────────────
VITE_API_URL=http://localhost:3000

# ── Auth Security ────────────────────────────────────────────
# 32+ char random key – production mein zaroor change karo
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_AUTH_ENC_KEY=change-this-to-a-strong-random-secret-key

# ── App Identity ─────────────────────────────────────────────
VITE_APP_NAME=Vensar Drive
VITE_APP_VERSION=1.0.0

# ── Upload Limits ─────────────────────────────────────────────
# Backend ke UPLOAD_MAX_SIZE se match karo (bytes)
VITE_UPLOAD_MAX_SIZE=104857600

# ── Request Timeout ───────────────────────────────────────────
VITE_REQUEST_TIMEOUT_MS=30000

# ── Signed URL Cache ──────────────────────────────────────────
VITE_SIGNED_URL_CACHE_MS=240000
